@AGENTS.md

---

## 모델 위임 전략

작업을 현재 모델이 전부 직접 수행하면 토큰 낭비가 크다.
단, sub agent도 별도 context window에서 토큰을 사용하므로 무조건 절약되는 방식은 아니며, 읽을 파일·수정 범위·출력 형식을 명확히 제한할 수 있을 때만 위임한다.

**작업을 쪼개서, 각 하위 작업에 필요한 최소 난이도의 모델에게 위임한다.**

모델 난이도 순서(낮음→높음): Haiku < Sonnet < Opus < Fable

하위 작업별 위임 기준:
- **Haiku까지면 충분**: 내용이 이미 확정되어 그대로 옮기기만 하면 되는 문서 작성, 포맷팅, 간단한 검색, lint/test 실행, 정형화된 반복 수정
- **Sonnet까지 필요**: 일반적인 코드 구현, 명확한 스펙이 있는 리팩터링/버그 수정, 애매한 지시를 해석해 반영해야 하는 문서 작성(초안 후 상위 모델 검수 권장)
- **Opus까지 필요**: 복잡한 디버깅, 여러 모듈에 걸친 설계 판단
- **Fable이 필요**: 프로젝트 방향성 제안, 아키텍처 결정, 원인 불명확한 문제의 원인 분석, 리스크 검토 등 고난도 판단

**판단 원칙**: "이 작업을 처리할 수 있는 가장 저렴한 모델은 무엇인가"를 먼저 묻는다.
현재 작업을 주도하는 모델이 직접 처리해야 한다고 판단되면, 왜 더 저렴한 모델(sub agent)로 안 되는지 먼저 설명한다.
